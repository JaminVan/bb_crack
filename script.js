$(document).ready(function() {
	// crack pro
	$('#components-menu__logo').prop('id', 'components-menu__user-name');
	// crack for customizer theme
	$('.customizer__pro').remove();
	// remove all 
	$('.unlock-all-items').remove();
});